# Report

**Name:** Ràm

**Session by:** Mr. Siddu

**Topics covered:**
- React useState Hook
- Controlled Components (textarea)
- Real-time State Updates
- String Operations (length, split, trim)

**Task implementation:**
- `App.jsx` — Textarea controlled via `useState`. On every keystroke `onChange` updates state. Character count uses `text.length`. Word count splits on whitespace regex `/\s+/` after trimming, returning 0 for empty input.
- `App.css` — Dark gradient glassmorphism UI with glowing stat numbers and a responsive layout.

**Repository link:** https://github.com/ThunderOoops/React-Character-Counter
